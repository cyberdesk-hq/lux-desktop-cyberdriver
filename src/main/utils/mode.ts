import { Mode } from '../../common';

export const MODE_PLACEHOLDER: Record<Mode, string> = {
  [Mode.Actor]: 'Add Instructions',
  [Mode.Thinker]: 'Add Instructions',
  [Mode.Tasker]: 'Choose a workflow',
};
